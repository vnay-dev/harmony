name: harmony
description: "A Shruti app (pitch drone) for everyone."
publish_to: 'none'

# Versions represent releases, not commits. See .cursor/rules/versioning.mdc.
version: 0.0.1+1

environment:
  sdk: ^3.10.9

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
